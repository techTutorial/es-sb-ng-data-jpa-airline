
-- h2 Database


DROP TABLE IF EXISTS EMPLOYEE_ENTITY;

create table EMPLOYEE_ENTITY (
   ID_EMPLOYEE BIGINT AUTO_INCREMENT PRIMARY KEY,
   FNAME_EMPLOYEE varchar(100) NOT NULL,
   LNAME_EMPLOYEE varchar(100) NOT NULL,
   EMAIL_EMPLOYEE varchar(100) NOT NULL
   --PRICE_EMPLOYEE NUMBER(10) default NULL,
   --AVAILABILITY_EMPLOYEE BOOL DEFAULT TRUE,
   --DATE_CREATION_EMPLOYEE TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
   --DATE_MODIFICATION_EMPLOYEE TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- drop table EMPLOYEE_ENTITY;
-- select * from EMPLOYEE_ENTITY;


